<%= turbo_stream.append "answers",
    partial: "answers/answer",
    locals: { answer: @answer, question: @question} %>

<%= turbo_stream.update "new_answer",
    partial: "answers/form",
    locals: { answer: Answer.new, question: @question} %>
