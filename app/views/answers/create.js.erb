$('.answer-errors').html("<%= escape_javascript(render('shared/errors', resource: @answer)) %>");

<% if @answer.persisted? %>
  $('.answers').append("<%= escape_javascript(render @answer) %>");
  $('.new-answer #answer_body').val('');
<% end %>
